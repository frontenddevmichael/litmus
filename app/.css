/* ══════════════════════════════════════════════════════════════
   LITMUS v2 — page.module.css
   Every original class preserved. Upgraded for premium feel.
   ══════════════════════════════════════════════════════════════ */

/* ── CSS Variables ─────────────────────────────────────────── */
:root {
    --litmus-bg: #080808;
    --litmus-surface: #111111;
    --litmus-surface2: #161616;
    --litmus-border: rgba(255, 255, 255, 0.07);
    --litmus-border-hi: rgba(255, 255, 255, 0.14);
    --litmus-text: #f0ede6;
    --litmus-muted: #888580;
    --litmus-muted2: #555250;
    --litmus-accent: #c8f542;
    --litmus-accent-dark: #98c235ed;
    --litmus-accent2: #a78bfa;
    --litmus-red: #ff6b6b;
    --litmus-radius: 14px;
    --litmus-radius-sm: 8px;
}

/* ── Base ──────────────────────────────────────────────────── */
.root {
    background: var(--litmus-bg);
    color: var(--litmus-text);
    font-family: 'DM Sans', sans-serif;
    min-height: 100vh;
    overflow-x: hidden;
    background-image:
        radial-gradient(ellipse 80% 60% at 50% -10%, rgba(200, 245, 66, 0.06) 0%, transparent 70%),
        url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)' opacity='0.03'/%3E%3C/svg%3E");
}

/* ══════════════════════════════════════════════════════════════
   NAV — Complete redesign
   Before: solid lime-green bar (felt like a banner ad)
   After:  transparent → glass blur on scroll, black text gone,
           logo has a logomark, status indicator, cleaner CTA
   ══════════════════════════════════════════════════════════════ */

.nav {
    display: flex;
    align-items: center;
    gap: 2rem;
    padding: 1.1rem 2.5rem;
    position: sticky;
    top: 0;
    z-index: 100;

    /* Default: transparent, no border */
    background: transparent;
    border-bottom: 1px solid transparent;
    transition:
        background 0.4s ease,
        border-color 0.4s ease,
        backdrop-filter 0.4s ease;
}

/* Scrolled state — glass effect */
.navScrolled {
    background: rgba(8, 8, 8, 0.82);
    border-bottom: 1px solid var(--litmus-border);
    backdrop-filter: blur(20px) saturate(180%);
    -webkit-backdrop-filter: blur(20px) saturate(180%);
}

/* Logo */
.navLogo {
    font-family: 'Syne', sans-serif;
    font-size: 1.35rem;
    font-weight: 700;
    letter-spacing: -0.04em;
    color: var(--litmus-text);
    margin-right: auto;
    text-decoration: none;
    display: flex;
    align-items: center;
    gap: 0.4rem;
    transition: color 0.2s;
}

.navLogo:hover {
    color: var(--litmus-accent);
}

.navLogoMark {
    color: var(--litmus-accent);
    font-size: 1.1rem;
    line-height: 1;
}

/* Nav links */
.navLinks {
    display: flex;
    gap: 0.25rem;
    list-style: none;
    margin: 0;
    padding: 0;
}

.navLink {
    font-size: 0.875rem;
    font-weight: 400;
    color: var(--litmus-muted);
    text-decoration: none;
    padding: 0.45rem 0.85rem;
    border-radius: 8px;
    transition: color 0.2s, background 0.2s;
}

.navLink:hover {
    color: var(--litmus-text);
    background: rgba(255, 255, 255, 0.05);
}

/* Right cluster */
.navRight {
    display: flex;
    align-items: center;
    gap: 1rem;
}

/* Live status badge */
.navStatus {
    display: flex;
    align-items: center;
    gap: 0.4rem;
    font-size: 0.72rem;
    font-weight: 500;
    letter-spacing: 0.04em;
    color: var(--litmus-muted2);
    border: 1px solid var(--litmus-border);
    padding: 0.3rem 0.7rem;
    border-radius: 999px;
}

.navStatusDot {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--litmus-accent);
    flex-shrink: 0;
    animation: statusPulse 2.4s ease-in-out infinite;
}

@keyframes statusPulse {
    0%, 100% {
        box-shadow: 0 0 0 0 rgba(200, 245, 66, 0.5);
    }

    50% {
        box-shadow: 0 0 0 4px rgba(200, 245, 66, 0);
    }
}

/* CTA */
.navCta {
    font-size: 0.82rem;
    font-weight: 600;
    letter-spacing: 0.01em;
    color: var(--litmus-bg);
    background: var(--litmus-accent);
    padding: 0.5rem 1.1rem;
    border-radius: 999px;
    text-decoration: none;
    transition: opacity 0.2s, transform 0.2s, box-shadow 0.2s;
    white-space: nowrap;
}

.navCta:hover {
    opacity: 0.88;
    transform: translateY(-1px);
    box-shadow: 0 4px 20px rgba(200, 245, 66, 0.3);
}

.navCtaDesktop {
    display: inline-flex;
}

/* Hamburger */
.hamburger {
    display: none;
    flex-direction: column;
    gap: 5px;
    background: none;
    border: none;
    cursor: pointer;
    padding: 6px;
    margin-left: auto;
}

.hamburger span {
    display: block;
    width: 22px;
    height: 1.5px;
    background: var(--litmus-text);
    border-radius: 2px;
    transform-origin: center;
}

/* Mobile menu */
.mobileMenu {
    position: absolute;
    top: calc(100% + 8px);
    right: 1.25rem;
    background: var(--litmus-surface);
    border: 1px solid var(--litmus-border-hi);
    border-radius: var(--litmus-radius);
    padding: 0.75rem;
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
    min-width: 220px;
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.6), 0 0 0 1px rgba(200, 245, 66, 0.04);
    z-index: 200;
}

.mobileMenuLink {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    font-size: 0.9rem;
    color: var(--litmus-muted);
    text-decoration: none;
    padding: 0.6rem 0.75rem;
    border-radius: var(--litmus-radius-sm);
    transition: background 0.2s, color 0.2s;
}

.mobileMenuLink:hover {
    background: var(--litmus-surface2);
    color: var(--litmus-text);
}

.mobileMenuNum {
    font-family: 'Syne', monospace;
    font-size: 0.65rem;
    color: var(--litmus-accent);
    font-weight: 700;
    letter-spacing: 0.06em;
}

/* ══════════════════════════════════════════════════════════════
   HERO — Fixed layout, added visual anchor, trust strip
   ══════════════════════════════════════════════════════════════ */

.hero {
    position: relative;
    min-height: 100svh;
    display: flex;
    align-items: center;
    overflow: hidden;
}

/* Giant watermark "A+" — the visual anchor that makes it feel editorial */
.heroWatermark {
    position: absolute;
    right: -2%;
    top: 50%;
    transform: translateY(-50%);
    font-family: 'Syne', sans-serif;
    font-size: clamp(16rem, 28vw, 30rem);
    font-weight: 900;
    letter-spacing: -0.08em;
    color: transparent;
    -webkit-text-stroke: 1px rgba(200, 245, 66, 0.06);
    line-height: 0.85;
    pointer-events: none;
    user-select: none;
    z-index: 0;
}

.heroScreens {
    position: relative;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 3rem;
    padding: 6rem 3rem 5rem;
    align-items: center;
    z-index: 1;
    width: 100%;
    max-width: 1200px;
    margin: 0 auto;
}

.heroContent {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 0;
    padding: 0;
    width: 100%;
}

/* Eyebrow pill */
.heroPill {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.72rem;
    font-weight: 500;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--litmus-accent);
    background: rgba(200, 245, 66, 0.07);
    border: 1px solid rgba(200, 245, 66, 0.18);
    padding: 0.35rem 0.85rem;
    border-radius: 999px;
    margin-bottom: 1.75rem;
}

.heroPillDot {
    width: 5px;
    height: 5px;
    border-radius: 50%;
    background: var(--litmus-accent);
    animation: statusPulse 2s ease-in-out infinite;
    flex-shrink: 0;
}

.heroTitle {
    font-family: 'Syne', sans-serif;
    /* Weight contrast: heavy title, light body — this is what makes it premium */
    font-weight: 800;
    font-size: clamp(2.4rem, 4.5vw, 4rem);
    line-height: 1.02;
    letter-spacing: -0.05em;
    color: var(--litmus-text);
    margin: 0 0 1.5rem;
    width: 100%;
    /* No more width: 120% — contained properly */
}

.heroTitleAccent {
    color: var(--litmus-accent);
    font-weight: 800;
    display: block;
}

.heroSub {
    font-size: 1rem;
    line-height: 1.8;
    color: var(--litmus-muted);
    max-width: 440px;
    margin: 0 0 2.25rem;
}

.heroCtas {
    display: flex;
    gap: 0.75rem;
    margin-bottom: 2rem;
    flex-wrap: wrap;
}

/* Trust strip under CTAs */
.heroTrust {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    flex-wrap: wrap;
}

.heroTrustItem {
    font-size: 0.75rem;
    color: var(--litmus-muted2);
}

.heroTrustDivider {
    width: 3px;
    height: 3px;
    border-radius: 50%;
    background: var(--litmus-muted2);
    flex-shrink: 0;
}

/* CTAs */
.ctaPrimary {
    font-size: 0.92rem;
    font-weight: 600;
    color: var(--litmus-bg);
    background: var(--litmus-accent);
    padding: 0.75rem 1.6rem;
    border-radius: 999px;
    text-decoration: none;
    transition: opacity 0.2s, transform 0.2s, box-shadow 0.2s;
    display: inline-flex;
    align-items: center;
    gap: 0.25rem;
}

.ctaPrimary:hover {
    opacity: 0.88;
    transform: translateY(-2px);
    box-shadow: 0 0 24px rgba(200, 245, 66, 0.35);
}

.ctaSecondary {
    font-size: 0.9rem;
    font-weight: 500;
    color: var(--litmus-text);
    background: transparent;
    border: 1px solid var(--litmus-border-hi);
    padding: 0.75rem 1.6rem;
    border-radius: 999px;
    text-decoration: none;
    transition: background 0.2s, border-color 0.2s;
}

.ctaSecondary:hover {
    background: var(--litmus-surface);
    border-color: rgba(255, 255, 255, 0.2);
}

/* Hero card */
.heroCardWrap {
    width: 100%;
    display: flex;
    justify-content: center;
    align-items: center;
}

.heroCard {
    background: var(--litmus-surface);
    border: 1px solid var(--litmus-border-hi);
    border-radius: 18px;
    overflow: hidden;
    box-shadow:
        0 0 0 1px rgba(200, 245, 66, 0.04),
        0 40px 80px rgba(0, 0, 0, 0.6),
        inset 0 1px 0 rgba(255, 255, 255, 0.05);
    transition: box-shadow 0.3s;
    width: 100%;
    max-width: 480px;
}

.heroCard:hover {
    box-shadow:
        0 0 0 1px rgba(200, 245, 66, 0.1),
        0 40px 80px rgba(0, 0, 0, 0.6),
        0 0 40px rgba(200, 245, 66, 0.06),
        inset 0 1px 0 rgba(255, 255, 255, 0.05);
}

.heroCardGlitch {
    filter: brightness(1.15) hue-rotate(5deg);
    transform: translateX(1px);
}

.heroCardBar {
    display: flex;
    align-items: center;
    gap: 6px;
    padding: 0.75rem 1rem;
    border-bottom: 1px solid var(--litmus-border);
    background: rgba(255, 255, 255, 0.02);
}

.heroCardDot {
    width: 10px;
    height: 10px;
    border-radius: 50%;
    opacity: 0.8;
}

.heroCardLabel {
    font-size: 0.72rem;
    color: var(--litmus-muted2);
    margin-left: auto;
    margin-right: auto;
    letter-spacing: 0.02em;
}

.heroCardBody {
    padding: 1.25rem 1.5rem;
    display: flex;
    flex-direction: column;
    gap: 0.85rem;
}

.heroCardInput {
    font-size: 0.85rem;
    color: var(--litmus-muted);
    font-style: italic;
    border-left: 2px solid var(--litmus-border-hi);
    padding-left: 0.75rem;
    margin: 0;
}

.heroCardScanRow {
    display: flex;
    justify-content: space-between;
    font-size: 0.75rem;
    color: var(--litmus-muted2);
    letter-spacing: 0.03em;
}

.heroCardScanDots {
    color: var(--litmus-accent);
}

.heroCardProgressBar {
    height: 3px;
    background: rgba(255, 255, 255, 0.07);
    border-radius: 99px;
    overflow: visible;
    position: relative;
}

.heroCardProgressFill {
    height: 100%;
    background: var(--litmus-accent);
    border-radius: 99px;
}

.heroCardResult {
    display: flex;
    align-items: center;
    gap: 0.85rem;
    background: rgba(200, 245, 66, 0.04);
    border: 1px solid rgba(200, 245, 66, 0.12);
    border-radius: var(--litmus-radius-sm);
    padding: 0.75rem 1rem;
}

.gradeBadge {
    font-family: 'Syne', sans-serif;
    font-size: 1.6rem;
    font-weight: 800;
    color: var(--litmus-accent);
    background: rgba(200, 245, 66, 0.1);
    border: 1.5px solid rgba(200, 245, 66, 0.25);
    border-radius: 10px;
    width: 52px;
    height: 52px;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    letter-spacing: -0.04em;
}

.heroCardResultTitle {
    font-size: 0.85rem;
    font-weight: 600;
    color: var(--litmus-text);
    margin: 0 0 2px;
}

.heroCardResultSub {
    font-size: 0.72rem;
    color: var(--litmus-muted);
    margin: 0;
}

.heroCardBars {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
}

.heroCardBarRow {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    font-size: 0.72rem;
    color: var(--litmus-muted);
}

.heroCardBarRow span:first-child {
    width: 130px;
    flex-shrink: 0;
    text-align: left;
}

.heroCardBarRow span:last-child {
    width: 34px;
    flex-shrink: 0;
    text-align: right;
}

.heroCardBarTrack {
    flex: 1;
    height: 3px;
    background: rgba(255, 255, 255, 0.07);
    border-radius: 99px;
    overflow: hidden;
}

.heroCardBarFill {
    height: 100%;
    border-radius: 99px;
}

/* ── Ticker ────────────────────────────────────────────────── */
.tickerWrap {
    width: 100%;
    overflow: hidden;
    border-top: 1px solid var(--litmus-border);
    border-bottom: 1px solid var(--litmus-border);
    padding: 0.85rem 0;
    position: relative;
    background: var(--litmus-surface);
    cursor: default;
}

.tickerWrap::before, .tickerWrap::after {
    content: '';
    position: absolute;
    top: 0;
    bottom: 0;
    width: 80px;
    z-index: 2;
    pointer-events: none;
}

.tickerWrap::before {
    left: 0;
    background: linear-gradient(90deg, var(--litmus-surface), transparent);
}

.tickerWrap::after {
    right: 0;
    background: linear-gradient(-90deg, var(--litmus-surface), transparent);
}

.tickerTrack {
    display: flex;
    gap: 0;
    white-space: nowrap;
    animation: ticker 22s linear infinite;
    transition: animation-duration 0.4s;
}

.tickerItem {
    font-size: 0.8rem;
    font-weight: 500;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    color: var(--litmus-muted);
    padding: 0 2.5rem;
    flex-shrink: 0;
    transition: color 0.2s;
}

.tickerItem:hover {
    color: var(--litmus-accent);
}

.tickerDot {
    color: var(--litmus-accent);
    margin-left: 2.5rem;
}

@keyframes ticker {
    from {
        transform: translateX(0);
    }

    to {
        transform: translateX(-50%);
    }
}

/* ── Stats Strip ───────────────────────────────────────────── */
.statsStrip {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    border-top: 1px solid var(--litmus-border);
    border-bottom: 1px solid var(--litmus-border);
}

.statItem {
    display: flex;
    flex-direction: column;
    align-items: center;
    padding: 2rem 1rem;
    border-right: 1px solid var(--litmus-border);
    position: relative;
    overflow: hidden;
    transition: background 0.3s;
}

.statItem:last-child {
    border-right: none;
}

.statItem::before {
    content: '';
    position: absolute;
    inset: 0;
    background: radial-gradient(ellipse at 50% 120%, rgba(200, 245, 66, 0.05), transparent 70%);
    opacity: 0;
    transition: opacity 0.3s;
}

.statItem:hover::before {
    opacity: 1;
}

.statValue {
    font-family: 'Syne', sans-serif;
    font-size: clamp(1.6rem, 3vw, 2.2rem);
    font-weight: 800;
    color: var(--litmus-accent);
    letter-spacing: -0.04em;
    line-height: 1;
    margin-bottom: 0.4rem;
}

.statLabel {
    font-size: 0.75rem;
    color: var(--litmus-muted);
    letter-spacing: 0.04em;
    text-transform: uppercase;
    font-weight: 500;
}

/* ── Sections ──────────────────────────────────────────────── */
.section {
    max-width: 1080px;
    margin: 0 auto;
    padding: 6rem 2rem;
}

.sectionLabel {
    font-size: 0.7rem;
    font-weight: 700;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--litmus-accent);
    margin: 0 0 0.75rem;
}

.sectionTitle {
    font-family: 'Syne', sans-serif;
    font-size: clamp(1.8rem, 4vw, 2.8rem);
    font-weight: 800;
    letter-spacing: -0.04em;
    color: var(--litmus-text);
    margin: 0 0 0.75rem;
    max-width: 640px;
}

.sectionTitle em {
    font-style: normal;
    color: var(--litmus-accent);
}

.sectionSub {
    font-size: 0.95rem;
    color: var(--litmus-muted);
    max-width: 480px;
    line-height: 1.7;
    margin: 0 0 3.5rem;
}

/* ── Steps Grid ────────────────────────────────────────────── */
.stepsGrid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
    gap: 1px;
    background: var(--litmus-border);
    border: 1px solid var(--litmus-border);
    border-radius: var(--litmus-radius);
    overflow: hidden;
    margin-top: 1rem;
}

.stepCard {
    background: var(--litmus-surface);
    padding: 2rem 1.75rem;
    transition: background 0.2s;
    position: relative;
    overflow: hidden;
}

.stepCard:hover {
    background: var(--litmus-surface2);
}

.stepCard::after {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 60%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(200, 245, 66, 0.04), transparent);
    transition: left 0.5s ease;
    pointer-events: none;
}

.stepCard:hover::after {
    left: 150%;
}

.stepNum {
    display: block;
    font-family: 'Syne', sans-serif;
    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 0.1em;
    color: var(--litmus-accent);
    margin-bottom: 1rem;
}

.stepTitle {
    font-family: 'Syne', sans-serif;
    font-size: 1.05rem;
    font-weight: 700;
    color: var(--litmus-text);
    margin: 0 0 0.5rem;
    letter-spacing: -0.02em;
    position: relative;
    display: inline-block;
}

.stepTitleLine {
    display: block;
    height: 1px;
    background: var(--litmus-accent);
    transform-origin: left;
    margin-top: 3px;
    opacity: 0.5;
}

.stepBody {
    font-size: 0.85rem;
    color: var(--litmus-muted);
    line-height: 1.7;
    margin: 0.5rem 0 0;
}

.stepCornerDot {
    position: absolute;
    bottom: 16px;
    right: 16px;
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--litmus-accent);
    opacity: 0.4;
}

/* ══════════════════════════════════════════════════════════════
   FEATURES — Redesigned
   Before: full-width lime bg + floating cards felt disconnected
   After:  contained inner column, cleaner card grid, proper
           spacing, rounded card borders restored
   ══════════════════════════════════════════════════════════════ */

.features {
    width: 100%;
    max-width: 100%;
    background: linear-gradient(180deg, transparent 0%, rgba(200, 245, 66, 0.025) 40%, transparent 100%);
    border-top: 1px solid var(--litmus-border);
    border-bottom: 1px solid var(--litmus-border);
    padding: 6rem 2rem;
    margin: 0;
    position: relative;
}

.featuresSectionInner {
    max-width: 1080px;
    margin: 0 auto 2.5rem;
}

.featuresGrid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 1px;
    background: var(--litmus-border);
    border: 1px solid var(--litmus-border);
    border-radius: var(--litmus-radius);
    overflow: hidden;
    max-width: 1080px;
    margin: 0 auto;
}

.featureCard {
    background: var(--litmus-surface);
    padding: 2rem 1.75rem;
    transition: background 0.25s;
    position: relative;
    overflow: hidden;
    cursor: default;
    perspective: 600px;
}

.featureCard:hover {
    background: var(--litmus-surface2);
}

.featureCard:hover .featureIcon {
    color: var(--litmus-accent);
    border-color: rgba(200, 245, 66, 0.3);
    background: rgba(200, 245, 66, 0.08);
}

.featureCard::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 1px;
    background: linear-gradient(90deg, transparent, var(--litmus-accent), transparent);
    opacity: 0;
    transition: opacity 0.3s;
}

.featureCard:hover::before {
    opacity: 0.6;
}

.featureIcon {
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1rem;
    font-weight: 700;
    color: var(--litmus-muted2);
    margin-bottom: 1rem;
    font-family: 'Syne', monospace;
    transition: color 0.25s, background 0.25s, border-color 0.25s;
    width: 36px;
    height: 36px;
    background: rgba(255, 255, 255, 0.04);
    border: 1px solid var(--litmus-border-hi);
    border-radius: 8px;
}

.featureTitle {
    font-family: 'Syne', sans-serif;
    font-size: 0.95rem;
    font-weight: 700;
    color: var(--litmus-text);
    margin: 0 0 0.5rem;
    letter-spacing: -0.02em;
}

.featureBody {
    font-size: 0.82rem;
    color: var(--litmus-muted);
    line-height: 1.75;
    margin: 0;
}

/* ── CTA Banner ────────────────────────────────────────────── */
.ctaBanner {
    margin: 0 auto 4rem;
    padding: 5rem 2rem;
    text-align: center;
    position: relative;
    overflow: hidden;
}

.ctaElectric {
    margin: auto;
}

.ctaBanner::before {
    content: '';
    position: absolute;
    top: -60px;
    left: 50%;
    transform: translateX(-50%);
    width: 500px;
    height: 200px;
    background: radial-gradient(ellipse, rgba(200, 245, 66, 0.07) 0%, transparent 70%);
    pointer-events: none;
}

.ctaBannerEyebrow {
    font-family: 'Syne', sans-serif;
    font-size: clamp(1.6rem, 4vw, 2.8rem);
    font-weight: 800;
    color: var(--litmus-muted);
    margin: 0;
    letter-spacing: -0.04em;
}

.ctaBannerTitle {
    font-family: 'Syne', sans-serif;
    font-size: clamp(2.2rem, 6vw, 4rem);
    font-weight: 800;
    color: var(--litmus-text);
    margin: 0 0 0.75rem;
    letter-spacing: -0.05em;
}

.ctaBannerSub {
    font-size: 0.95rem;
    color: var(--litmus-muted);
    margin: 0 auto 2.25rem;
    max-width: 360px;
}

/* ── Footer ────────────────────────────────────────────────── */
.footer {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 1.5rem 2.5rem;
    border-top: 1px solid var(--litmus-border);
}

.footerRight {
    font-size: 0.78rem;
    color: var(--litmus-muted2);
}

/* ══════════════════════════════════════════════════════════════
   RESPONSIVE
   ══════════════════════════════════════════════════════════════ */

@media (max-width: 900px) {
    .nav {
        padding: 1rem 1.25rem;
    }

    .navLinks {
        display: none;
    }

    .navStatus {
        display: none;
    }

    .navCtaDesktop {
        display: none;
    }

    .hamburger {
        display: flex;
    }

    .heroScreens {
        grid-template-columns: 1fr;
        padding: 4rem 1.5rem 3rem;
        gap: 2.5rem;
    }

    .heroContent {
        align-items: center;
        text-align: center;
    }

    .heroSub {
        text-align: center;
    }

    .heroTrust {
        justify-content: center;
    }

    .heroCtas {
        justify-content: center;
    }

    .heroWatermark {
        font-size: clamp(10rem, 40vw, 18rem);
        right: -5%;
        top: 55%;
    }

    .statsStrip {
        grid-template-columns: repeat(2, 1fr);
    }

    .statItem:nth-child(2) {
        border-right: none;
    }

    .statItem:nth-child(3) {
        border-top: 1px solid var(--litmus-border);
    }

    .statItem:nth-child(4) {
        border-top: 1px solid var(--litmus-border);
        border-right: none;
    }

    .featuresGrid {
        grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
    }

    .features {
        padding: 4rem 1.5rem;
    }

    .featuresSectionInner {
        margin-bottom: 2rem;
    }
}

@media (max-width: 640px) {
    .heroTitle {
        font-size: clamp(1.9rem, 8vw, 2.8rem);
    }

    .heroSub {
        font-size: 0.9rem;
    }

    .heroCtas {
        flex-direction: column;
        align-items: stretch;
        width: 100%;
        max-width: 300px;
        margin-left: auto;
        margin-right: auto;
    }

    .ctaPrimary, .ctaSecondary {
        text-align: center;
        justify-content: center;
    }

    .heroWatermark {
        display: none;
    }

    .statsStrip {
        grid-template-columns: repeat(2, 1fr);
    }

    .section {
        padding: 4rem 1.25rem;
    }

    .stepsGrid, .featuresGrid {
        grid-template-columns: 1fr;
    }

    .ctaBanner {
        margin: 0 1rem 3rem;
        padding: 3.5rem 1.5rem;
    }

    .ctaElectric {
        width: 95% !important;
    }

    .footer {
        padding: 1.25rem;
        flex-direction: column;
        gap: 0.5rem;
        text-align: center;
    }

    .mobileMenu {
        right: 0.5rem;
        left: 0.5rem;
        min-width: unset;
    }
}

@media (max-width: 400px) {
    .navLogo {
        font-size: 1.1rem;
    }

    .heroTitle {
        font-size: clamp(1.5rem, 9vw, 2.2rem);
    }

    .statsStrip {
        grid-template-columns: 1fr 1fr;
    }
}